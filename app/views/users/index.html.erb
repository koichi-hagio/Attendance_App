<% provide(:title, "All Users") %>
  <h1>ユーザー一覧</h1>

<div class="col-md-10">
  <div class="pull-left">
    <%= form_with url: users_path, method: :get, local: true do |f| %>
      <%= f.file_field :upload_file %>
    <% end %>
  </div>
</div>

<!-- f.submit "ファイルを選択", class: "btn btn-sm btn-" col-md-offset-1 -->